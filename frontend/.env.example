# Frontend Environment Variables

# API URL - Change this to your backend URL in production
# For local development, leave empty (will use localhost:8000)
# For production (Render), set to your backend URL
VITE_API_URL=
